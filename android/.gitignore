# Everything in android/ is either generated by `bubblewrap build` from
# twa-manifest.json, a signing secret, or a build output. Only the TWA manifest
# and the helper script are tracked — run ./build-apk.sh to regenerate the rest.
*
!.gitignore
!twa-manifest.json
!build-apk.sh
